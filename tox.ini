[tox]
isolated_build = true
envlist = py38

[testenv]
allowlist_externals =
    poetry
    flake8
commands =
    poetry install -v
    poetry run pytest --cov-config=.coveragerc --cov=snp_pred --cov-report=html --cov-fail-under 75 tests/
    flake8