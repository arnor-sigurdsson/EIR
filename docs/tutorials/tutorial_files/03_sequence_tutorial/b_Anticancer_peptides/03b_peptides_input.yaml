input_info:
  input_source: Anticancer_Peptides/breast_cancer_train
  input_name: peptide_sequences
  input_type: sequence

input_type_info:
        model_type: sequence-default
        max_length: "max"
        split_on: ""

model_config:
        num_heads: 8
        dropout: 0.2

interpretation_config:
  num_samples_to_interpret: 30
  interpretation_sampling_strategy: random_sample
