basic_experiment:
  batch_size: 32
  memory_dataset: false
  n_epochs: 10
  output_folder: runs/test_streaming_run
evaluation_checkpoint:
  sample_interval: 100
  checkpoint_interval: 100
data_preparation:
  streaming_setup_samples: 5000
accelerator:
  hardware: cpu