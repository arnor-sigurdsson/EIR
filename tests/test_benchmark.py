def test_flatten():
    return
    # random_snp_sample = np.random.permutation(array.shape[-1])[:50]
    # for random_snp in random_snp_sample:
    #     random_sample = np.random.randint(0, array.shape[0])
    #
    #     height = array.shape[1]
    #     original_column = array[random_sample, :, random_snp]
    #     rs_idx_flat = random_snp * height
    #     flattened_part = flattened_arr[random_sample,
    #                      rs_idx_flat:rs_idx_flat + height]
    #
    #     assert (original_column == flattened_part).all()
